#include <stdio.h>

int main()
{
    int number = 0;          // Integer (4 bytes)
    char character = 'w';    // Character (1 byte)
    float flutuant = 0.2f;   // Floating point number (4 bytes)
    double precised = 0.3;   // Double-precision Floating point number (8 bytes)
    return 0;
}