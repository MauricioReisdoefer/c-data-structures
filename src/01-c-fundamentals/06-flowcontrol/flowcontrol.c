#include <stdio.h>

int main()
{
    int number = 5;
    if (number < 10) {
        printf("Number is lower than 10");
    }
    else {
        printf("Number is bigger than 10");
    }
    return 0;
}